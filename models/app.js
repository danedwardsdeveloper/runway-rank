// const pool = require("./database.js");

// const create = (description) => pool.query("INSERT INTO Ratings (description) VALUES ($1) RETURNING *", [description]);

// const get = () => pool.query("SELECT * FROM Ratings");

// const remove = (id) => pool.query("DELETE FROM Ratings WHERE photo_id = $1", [id]);

// module.exports = {
//   create,
//   get,
//   remove,
// };
